name: AQI Predictor CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # 1.5. Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
          cache: 'pip'

      # 1.6. Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 1.7. Train model
      - name: Train model
        run: |
          python train.py

      # 2. Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 3. Build and push Docker image
      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/aqi-predictor:latest

      # 4. Deploy to Kubernetes
      - name: Deploy to Kubernetes
        run: |
          # Dynamically update deployment configuration for production cluster
          sed -i "s|image: aqi-predictor:latest|image: ${{ secrets.DOCKER_USERNAME }}/aqi-predictor:latest|g" k8s/deployment.yaml
          sed -i "s|imagePullPolicy: Never|imagePullPolicy: Always|g" k8s/deployment.yaml
          kubectl apply -f k8s/